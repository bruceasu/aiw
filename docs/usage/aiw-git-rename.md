# aiw git rename

Short: Rename files or paths tracked by git with helper semantics.

Description:
Convenience wrapper for renaming tracked files and updating the index and history appropriately.

Usage:
aiw git rename <old> <new>

Examples:
- aiw git rename oldname newname

For full help run: generate_git_docs.py -h