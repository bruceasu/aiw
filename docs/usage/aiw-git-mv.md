# aiw git mv

Short: Move or rename files or paths tracked by git with helper semantics.

Description:
Convenience wrapper for moving or renaming tracked files and updating the index and history appropriately.

Usage:
aiw git mv <old> <new>

Examples:
- aiw git mv oldname newname

For full help run: generate_new_plugin_docs.py -h
