# aiw git track

Short: Show or set upstream tracking for the current branch.

Description:
With no args, shows the current branch upstream. With one arg, sets the upstream branch (git branch --set-upstream-to).

Usage:
aiw git track [remote/branch]

Examples:
- aiw git track
- aiw git track origin/main

For full help run: generate_git_docs.py -h