# aiw git ca

Short: Amend the last commit, include all staged changes.

Description:
Runs git commit -a --amend to amend the previous commit with staged and unstaged changes.

Usage:
aiw git ca

Examples:
- aiw git ca

For full help run: generate_git_docs.py -h