# aiw git update

Short: Fetch and rebase or pull updates from remote.

Description:
Fetches updates from the remote and rebases the current branch onto its upstream by default. Use --merge to perform a merge pull.

Usage:
aiw git update [--merge]

Examples:
- aiw git update
- aiw git update --merge

For full help run: generate_git_docs.py -h