# aiw git get-file-from

Short: Extract a file version from another branch/commit.

Description:
Retrieves a file from a different branch or commit and writes it into the working tree.

Usage:
aiw git get-file-from <commit|branch> <path>

Examples:
- aiw git get-file-from origin/main path/to/file

For full help run: generate_git_docs.py -h