# aiw git caf

Short: Amend the last commit without changing the message.

Description:
Runs git commit -a --amend --no-edit to amend the previous commit while preserving its message.

Usage:
aiw git caf

Examples:
- aiw git caf

For full help run: generate_git_docs.py -h