# aiw git unpulled

Short: Show commits present on upstream but not pulled locally.

Description:
Shows commits available on the upstream branch that are not present locally.

Usage:
aiw git unpulled

Examples:
- aiw git unpulled

For full help run: generate_git_docs.py -h