# aiw-github currently reads authentication from the environment.
#
# GITHUB_TOKEN=your-token
#
# The plugin does not read this file automatically. Keep credentials out of
# repository files and use GITHUB_TOKEN for local or CI execution.
